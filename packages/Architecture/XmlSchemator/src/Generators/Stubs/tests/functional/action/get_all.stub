<?php

namespace App\Containers\{{section-name}}\{{container-name}}\UI\API\Tests\Functional;

use App\Containers\{{section-name}}\{{container-name}}\Models\{{model-name}};
use App\Containers\{{section-name}}\{{container-name}}\Tests\ApiTestCase;

/**
 * Class {{class-name}}.
 *
 * @group api
 * @group {{~section-name}}
 * @group {{~section-name}}_api
 * @group {{~container-name}}
 * @group {{~container-name}}_api
 * @group {{~model-name}}
 * @group {{~collection-name}}
 * @group {{~collection-name}}_api
 */
class {{class-name}} extends ApiTestCase
{
    // the endpoint to be called within this test (e.g., get@v1/users)
    protected string $endpoint = '{{method}}@v{{endpoint-version}}/{{endpoint-url}}';

    // fake some access rights
    protected array $access = [
        'permissions' => '',
        'roles'       => '',
    ];

    public function test_{{~collection-name}}_can_be_fetched_in_bulk_from_api(): void
    {
        $data = [
            // 'key' => 'value',
        ];

        // send the HTTP request
        $response = $this->makeCall();

        // assert the response status
        $response->assertStatus(200);

        $response->assertJsonCount(env('PAGINATION_LIMIT_DEFAULT'), 'data');

        // make other asserts here
    }
}
