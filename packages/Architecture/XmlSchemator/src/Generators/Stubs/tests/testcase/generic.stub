<?php

namespace App\Containers\{{section-name}}\{{container-name}}\Tests;

use App\Ship\Parents\Tests\PhpUnit\TestCase as ShipTestCase;
use Illuminate\Support\Arr;

/**
 * Class {{class-name}}.
 *
 * This is the container Main TestCase class. Use this class to add your container specific helper functions.
 */
class {{class-name}} extends ShipTestCase
{
    /**
     * Returns the migrate paths
     *
     * @param  array|string  $class
     * @return $this
     */
    public function getMigrationPaths(): array
    {
        return [
            'app/Containers/{{section-name}}/{{container-name}}/Data/Migrations',
        ];
    }

    /**
     * Seed a given database connection.
     *
     * @return $this
     */
    public function reSeed()
    {
        $seederCommands = config('{{~section-name}}-{{~container-name}}.testing.seeders');

        foreach (Arr::wrap($seederCommands) as $seederCommand) {
            $this->artisan($seederCommand, ['--no-interaction' => true]);
        }

        return $this;
    }
}
